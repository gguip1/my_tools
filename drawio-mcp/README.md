# Draw.io MCP 설정

Claude Code에서 Draw.io 다이어그램을 생성/편집할 수 있게 해주는 MCP 설정.

## 설치

```bash
npx degit gguip1/my_tools/drawio-mcp ./drawio-mcp
cd drawio-mcp
cp .mcp.json.example .mcp.json
```

## 사용법

설정 완료 후 Claude Code에서 Draw.io 다이어그램 관련 작업 가능.
